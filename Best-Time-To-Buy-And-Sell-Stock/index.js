var stocks = [7, 1, 5, 3, 6, 4];
var maxProfit = function (prices) {
    var max = Math.max(...stocks);
    var min = Math.min(...stocks);

    if (min != )
        if (stocks.indexOf(max) > stocks.indexOf(min)) {
            return max - min;
        }
}